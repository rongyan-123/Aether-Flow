上次对话记录 — 2026-06-15（第6轮）
================================================================================

【本轮任务】
修复修仙日志（经验库）两个 Bug + 添加 E2E 测试用例 + CLAUDE.md 规则

【Bug 1 — 发送按钮灰色】
根因: ChatPanel 的 input 是组件内 useState("")，日志→聊天导航时组件卸载重挂载，input 丢失。
修复: 在 zustand store 添加 pendingInput 字段，journal 跳转前 setPendingInput(content)，
     ChatPanel mount 时读取 pendingInput 并填入 input → send 按钮正常可用。

【Bug 2 — 筛选标签无 UI 反馈】
根因: journal-panel 原版没有筛选标签 UI。
修复: 新增 4 个筛选标签（全部/成功经验/失败教训/踩坑点），选中态 amber 边框+背景，
     未选中态 zinc 样式，过滤逻辑完整。同时扩展 entry_type 支持 success/failure/pitfall。

【已完成的修改】

1. src/stores/game.ts — 新增 pendingInput
   - state 添加 pendingInput: string（默认""）
   - action setPendingInput(input: string)
   - resetGame 中包含 pendingInput 重置
   - 不加入 partialize（无需持久化）

2. src/components/chat-panel.tsx — 读取 pendingInput
   - useEffect 监听 pendingInput，有值时填入 input 并清空

3. src/components/journal-panel.tsx — 完全重写
   - 新增 FILTERS 数组 + filter useState
   - 筛选标签栏，选中态视觉反馈
   - 每个日志条目右下角"聊这个"按钮
   - 点击 → setPendingInput + setCurrentView("chat")
   - 扩展 typeConfig 支持 success(emerald)/failure(orange)/pitfall(purple)

4. CLAUDE.md — 新增规则 8.1
   每个 UI 按钮必须测试完整流程：可见→可点击→点击后状态变化→最终结果
   特别注意 disabled 状态的按钮要在输入有内容后验证重新变为 enabled

5. e2e/game.spec.ts — 新增 ~120 行测试
   Journal 测试块：6 个筛选标签测试 + 4 个聊天按钮测试 + 1 个发送启用测试

【技术要点】
- Zustand store 的 pendingInput 实现跨组件通信（无需 URL 参数）
- Playwright 移动端测试：getByText/locator 需要 .filter({ visible: true }).first()
  因为 Tailwind hidden md:flex + md:hidden 会在 DOM 中同时渲染两套布局
- getByRole 默认 exclude hidden elements，无需额外处理

【当前状态】
- 两个 Bug 已修复，浏览器验证通过
- E2E 测试已添加，待全部跑通
- 待 git commit
